<div class="row">
  <h1><%= @contact.first_name %> <%= @contact.last_name %></h1>

  <p>  <%= link_to "Edit", edit_company_contact_path(@company, @contact), :class => "btn btn-warning" %>
  <%= link_to 'Delete', company_contact_path(@company, @contact), :data => {:confirm => "Are you sure?", :method => "delete"}, :class => "btn btn-danger" %></p>
</div>
